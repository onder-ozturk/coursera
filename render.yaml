services:
  - type: web
    name: coursehub-academy
    env: node
    branch: main
    buildCommand: CI=1 NEXT_DISABLE_TURBOPACK=1 npm run build
    startCommand: npm run start
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: "20"
